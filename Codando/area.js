function perimetro(numero) {
    var raio = 3.14*numero*2;
    return raio;
};
perimetro(5);

function area (raio) {
    var resultado = 3.14*raio*raio;
    return resultado
}

area()

//function usada para retornar perimetro e a area de um circulo atraves do raio